openapi: 3.0.1
info:
  title: Cahsper API
  description: |
    # Description
    This API documentation regarding OpenAPI specification. Generated by [ReDoc](https://github.com/Rebilly/ReDoc)
  version: 0.0.1
externalDocs:
  description: Source code
  url: 'https://github.com/YoshinoriN/cahsper'
servers:
  - url: http://example.com/
paths:
  /status:
    get:
      summary: Get API server status
      responses:
        200:
          description: Response when Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: operational
  /users:
    get:
      summary: Get all users
      responses:
        200:
          description: Response when Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/user'
    post:
      summary: Create a new user
      description: |
        Create a new user with the AWS Cognito username attribute
      security:
        - bearer:
          - write
      responses:
        201:
          description: Create a user succeeded
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/user'
        400:
          description: Payload is wrong
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/errorMessage'
        401:
          description: Unauthorized
  /users/{userName}:
    get:
      summary: Get a single user
      responses:
        200:
          description: Response when Success
          content:
            application/json:
              schema:
                type: object
                $ref: '#/components/schemas/user'
        404:
          description: user not found
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/errorMessageNotFound'
  /users/{userName}/comments:
    get:
      summary: Get comments by user
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
          description: The numbers of items to return. Max is 100.
        - in: query
          name: from
          schema:
            type: integer
          description: Range of comment created time. UnixTime.
        - in: query
          name: to
          schema:
            type: integer
          description: Range of comment created time. UnixTime.
        - in: query
          name: order
          schema:
            type: string
          description: Determines whether the search result returned is the latest or oldest.
      responses:
        200:
          description: Response when Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/comment'
    post:
      summary: Create a new comment
      security:
        - bearer:
          - write
      responses:
        201:
          description: Create a comment succeeded
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/comment'
        400:
          description: Payload is wrong
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/errorMessage'
        401:
          description: Unauthorized
        404:
          description: url path not found
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/errorMessageNotFound'
  /comments:
    get:
      summary: Get all users comments
      responses:
        200:
          description: Response when Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/comment'
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
          description: The numbers of items to return. Max is 100.
        - in: query
          name: from
          schema:
            type: integer
          description: Range of comment created time. UnixTime.
        - in: query
          name: to
          schema:
            type: integer
          description: Range of comment created time. UnixTime.
        - in: query
          name: order
          schema:
            type: string
          description: Determines whether the search result returned is the latest or oldest.
components:
  schemas:
    user:
      type: object
      properties:
        name:
          type: string
          example: JohnDoe
        createdAt:
          type: long
          description: Unix time
          example: 1567814290
    comment:
      type: object
      properties:
        id:
          type: integer
          format: int
          example: 1
        user:
          type: string
          example: JohnDoe
        comment:
          type: string
          example: "Hello Cahsper!!"
        createdAt:
          type: long
          description: Unix time
          example: 1567814290
    errorMessage:
      type: object
      properties:
        message:
          type: string
          example: Something is wrong
    errorMessageNotFound:
      type: object
      properties:
        message:
          type: string
          example: NotFound
  securitySchemes:
    bearer:
      type: http
      scheme: bearer
      format: JWT
      description: |
        Use AWS Cognito JWT for authorization and authentication. [Please see AWS document](https://docs.aws.amazon.com/cognito/latest/developerguide/what-is-amazon-cognito.html).
      flows:
        authorizationCode:
          scopes:
            write:
            read:
